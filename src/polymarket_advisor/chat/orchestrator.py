"""Profile -> ranking -> explanation."""
from typing import Any


async def run_chat_flow(profile: str, signals: list[dict[str, Any]]) -> dict[str, Any]:
    """Placeholder: orchestrate ranking + explanation."""
    return {"profile": profile, "ranking": signals, "explanation": ""}
